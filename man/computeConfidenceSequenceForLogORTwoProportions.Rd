% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe2x2Test.R
\name{computeConfidenceSequenceForLogORTwoProportions}
\alias{computeConfidenceSequenceForLogORTwoProportions}
\title{Confidence sequence for the log odds ratio}
\usage{
computeConfidenceSequenceForLogORTwoProportions(
  ya,
  yb,
  confidenceBoundGridPrecision,
  logORConfidenceSearchBounds,
  saviDesign
)
}
\arguments{
\item{ya, yb}{Number of successes in groups A and B in each data block.}

\item{confidenceBoundGridPrecision}{Number of candidate values on each side
of zero. Zero itself is not a candidate, so a bound of exactly zero is
never reported; the grid holds \code{2 * confidenceBoundGridPrecision} values.}

\item{logORConfidenceSearchBounds}{Positive finite lower and upper bounds
for the absolute candidate log odds ratios.}

\item{saviDesign}{A \code{saviDesign} returned by
\code{\link[=designSaviTwoProportions]{designSaviTwoProportions()}}.}
}
\value{
A data frame with \code{block}, \code{lowerBound}, and \code{upperBound}. A bound is
infinite until its one-sided sequence rejects a candidate and is \code{NA} if
every candidate on that side has been rejected.
}
\description{
Constructs a symmetric candidate grid from the supplied resolution and
search bounds; the grid excludes zero. The log odds ratio is
\code{logit(thetaB) - logit(thetaA)}. Two one-sided families are inverted, each
at \code{alpha / 2}: the lower family tests \code{logOR <= candidate}, the upper
family \code{logOR >= candidate}, and both run over the full signed grid.
}
\details{
The function walks through the data one block at a time. At every block
the predictor learned from the earlier blocks lies inside one family's
null for each candidate, where that family's likelihood-ratio increment is
zero, and outside the other family's null, where the predictor is
projected onto the candidate and the increment is added to that family's
running log e-process. Each family keeps a running intersection: a
candidate leaves a family's set once its log e-process reaches
\code{log(2 / alpha)} and is never projected again. The lower bound after a
block is the smallest candidate still in the lower family's set and the
upper bound the largest candidate still in the upper family's set.
}
