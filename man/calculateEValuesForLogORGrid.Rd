% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe2x2Test.R
\name{calculateEValuesForLogORGrid}
\alias{calculateEValuesForLogORGrid}
\title{Calculate e-processes over a grid of log odds ratios}
\usage{
calculateEValuesForLogORGrid(
  ya,
  yb,
  na,
  nb,
  priorParameters,
  logORGrid,
  bound = c("lower", "upper")
)
}
\arguments{
\item{ya, yb}{Number of successes in groups A and B in each data block.}

\item{na, nb}{Number of observations in groups A and B in each data block.
A scalar is recycled over all blocks.}

\item{priorParameters}{Named list with \code{betaA1}, \code{betaA2}, \code{betaB1}, and
\code{betaB2} for the Turner predictor.}

\item{logORGrid}{Finite candidate log odds ratios.}

\item{bound}{Whether to test the one-sided null \code{logOR <= candidate}
for a lower bound or \code{logOR >= candidate} for an upper bound.}
}
\value{
A list containing \code{logOR} and \code{logEProcesses}. Rows of the
log e-process matrix are data blocks and columns are candidate log odds
ratios.
}
\description{
For every candidate log odds ratio, computes the Turner likelihood-ratio
process whose denominator is the reverse information projection onto the
corresponding one-sided null. The log odds ratio is
\code{logOR = logit(thetaB) - logit(thetaA)}. Lower-bound processes use the null
\code{logOR <= candidate}; upper-bound processes use the null
\code{logOR >= candidate}. Either direction accepts signed candidates.
}
