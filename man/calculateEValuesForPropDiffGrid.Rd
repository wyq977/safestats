% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe2x2Test.R
\name{calculateEValuesForPropDiffGrid}
\alias{calculateEValuesForPropDiffGrid}
\title{Calculate e-processes over a grid of proportion differences}
\usage{
calculateEValuesForPropDiffGrid(
  ya,
  yb,
  na,
  nb,
  priorParameters,
  gridSize = 100
)
}
\arguments{
\item{ya, yb}{Number of successes in groups A and B in each data block.}

\item{na, nb}{Number of observations in groups A and B in each data block.
A scalar is recycled over all blocks.}

\item{priorParameters}{Named list with \code{betaA1}, \code{betaA2}, \code{betaB1}, and
\code{betaB2} for the Turner predictor.}

\item{gridSize}{Number of candidate proportion differences on each side of
zero. The grid is symmetric, never contains zero, and holds
\code{2 * gridSize} values in the open interval \verb{(-1, 1)}.}
}
\value{
A list containing \code{propDiff} and \code{logEProcesses}. Rows of the log
e-process matrix are data blocks and columns are candidate proportion
differences.
}
\description{
For every candidate \code{propDiff = thetaB - thetaA}, computes the Turner
likelihood-ratio process whose denominator is the reverse information
projection onto that candidate null curve. Every process is calculated once
over the complete data sequence and returned on the log scale.
}
