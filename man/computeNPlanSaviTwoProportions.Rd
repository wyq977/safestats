% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe2x2Test.R
\name{computeNPlanSaviTwoProportions}
\alias{computeNPlanSaviTwoProportions}
\title{Simulate worst-case stopping times for two proportions}
\usage{
computeNPlanSaviTwoProportions(
  na,
  nb,
  propDiff = NULL,
  logOR = NULL,
  alpha = 0.05,
  beta = 0.2,
  priorParameters = NULL,
  nSim = 1000,
  maxBlocks = 10000,
  thetaGridSize = 8L,
  nBoot = 1000,
  gridSize = 1000L
)
}
\arguments{
\item{na, nb}{Number of observations in groups A and B per block.}

\item{propDiff}{Data-generating proportion difference \code{thetaB - thetaA}.
Supply exactly one of \code{propDiff} and \code{logOR}.}

\item{logOR}{Data-generating value \code{logit(thetaB) - logit(thetaA)}.
Supply exactly one of \code{propDiff} and \code{logOR}.}

\item{alpha}{E-process rejection threshold is \code{1 / alpha}.}

\item{beta}{Target type-II error used to select the stopping-time quantile.}

\item{priorParameters}{Optional Turner Beta prior parameters.}

\item{nSim}{Number of simulated paths at each baseline probability.}

\item{maxBlocks}{Maximum number of blocks simulated per path.}

\item{thetaGridSize}{Number of equally spaced feasible baseline theta pairs
generated for this effect.}

\item{nBoot}{Number of nonparametric bootstrap samples used to estimate the
standard error of the worst-case stopping-time quantile; must be at least
two.}

\item{gridSize}{Number of points in the restricted numerator's posterior
grid. It should match the \code{gridSize} the test itself will use; lowering it
trades numerator accuracy for simulation speed.}
}
\value{
A list with the worst-case baseline probabilities, stopping-time
quantiles, and \code{thetaGridSize} by \code{nSim} matrices containing
the per-path stopping results. Non-crossing paths have stopping time \code{Inf}
and stopping e-value \code{NA}.
\code{worstCaseStoppingTimeBootstrapSe} is the bootstrap standard error of the
worst-case row's quantile, and \code{worstCaseStoppingTimeTwoSe} is twice that
standard error.
}
\description{
Simulates the Turner e-process under either a fixed proportion difference or
a fixed log odds ratio over a grid of feasible baseline probabilities. A
path that does not cross \code{1 / alpha} within \code{maxBlocks} has stopping time
\code{Inf} and stopping e-value \code{NA}.
}
\details{
The simulated numerator is restricted to the supplied effect, so the
stopping times describe the e-process that
\code{\link[=turnerEProcess]{turnerEProcess()}} runs with the same \code{restriction} and \code{delta} rather than
the unrestricted one.

The baseline grid is a separate simulation device: it ranges over the values
of \code{thetaA} for which both probabilities implied by the effect are in
\verb{(0, 1)}. The worst-case stopping time is the largest empirical
\code{(1 - beta)} quantile over this grid. The bootstrap resamples only the
already-identified worst-case row: with \code{nSim} paths already spent
on that theta pair, its own Monte Carlo noise is the relevant uncertainty.
To refine which baseline is worst, increase \code{thetaGridSize} rather than
re-deriving the worst case inside the bootstrap.
}
