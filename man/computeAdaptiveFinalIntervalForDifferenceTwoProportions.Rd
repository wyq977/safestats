% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe2x2Test.R
\name{computeAdaptiveFinalIntervalForDifferenceTwoProportions}
\alias{computeAdaptiveFinalIntervalForDifferenceTwoProportions}
\title{Heuristic adaptive final interval for a difference between two proportions}
\usage{
computeAdaptiveFinalIntervalForDifferenceTwoProportions(
  ya,
  yb,
  precision = 1e-08,
  saviDesign
)
}
\arguments{
\item{ya, yb}{Number of successes in groups A and B in each data block.}

\item{precision}{Maximum width of each retained/rejected endpoint bracket.}

\item{saviDesign}{A \code{saviDesign} returned by
\code{\link[=designSaviTwoProportions]{designSaviTwoProportions()}}.}
}
\value{
A list with conservative \code{lowerBound} and \code{upperBound}, the inner
retained/rejected endpoint brackets, \code{achievedPrecision}, \code{status}, and
diagnostics for every evaluated candidate. \code{heuristic} is always \code{TRUE}:
the connected-interval assumption has not been established for this
e-process.
}
\description{
Computes a final, running-intersection interval for \code{thetaB - thetaA} using
chunked e-process evaluations and adaptive endpoint bisection. Each queried
difference is evaluated from the first data block, so newly queried values
use their complete evidence history. This is a numerical heuristic: it
assumes the final retained set is one connected interval and does not prove
that property.
}
