% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe2x2Test.R
\name{simulateWorstCaseStoppingTimeLinearDifference}
\alias{simulateWorstCaseStoppingTimeLinearDifference}
\title{Simulate the worst-case stopping time for a linear difference}
\usage{
simulateWorstCaseStoppingTimeLinearDifference(
  na,
  nb,
  difference,
  alpha = 0.05,
  beta = 0.2,
  priorParameters = NULL,
  nSimulations = 1000,
  maxBlocks = 10000,
  gridSize = 8,
  nBoot = 1000
)
}
\arguments{
\item{na, nb}{Number of observations in groups A and B per block.}

\item{difference}{Data-generating difference \code{thetaB - thetaA}.}

\item{alpha}{E-process rejection threshold is \code{1 / alpha}.}

\item{beta}{Target type-II error used to select the stopping-time quantile.}

\item{priorParameters}{Optional Turner Beta prior parameters.}

\item{nSimulations}{Number of simulated paths at each baseline probability.}

\item{maxBlocks}{Maximum number of blocks simulated per path.}

\item{gridSize}{Number of equally spaced feasible baseline probabilities.}

\item{nBoot}{Number of nonparametric bootstrap samples used to estimate the
standard error of the worst-case stopping-time quantile; must be at least
two.}
}
\value{
A list with the worst-case baseline probabilities, stopping-time
quantiles, and per-path stopping results. \code{crossed} records whether a path
crossed the threshold. Non-crossing paths have stopping time \code{Inf} and
stopping e-value \code{NA} in \code{eValuesAtStopping}.
\code{worstCaseStoppingTimeBootstrapSe} is the bootstrap standard error of the
selected worst-case quantile, and \code{worstCaseStoppingTimeTwoSe} is twice
that standard error.
}
\description{
Simulates the Turner e-process under \code{thetaB - thetaA = difference} over a
grid of feasible baseline probabilities. A path that does not cross
\code{1 / alpha} within \code{maxBlocks} has stopping time \code{Inf}.
}
\details{
The baseline grid is a simulation device, not an alternative restriction.
It ranges over values of \code{thetaA} for which both probabilities are in
\verb{[0, 1]}.
}
